---
"ado-preflight-ui": patch
---

Vendor kubernetes.core and redhat.openshift from the lab Hub into the preflight collections directory, and install the Python Kubernetes client in the UI image so Install AAP can call kubernetes.core.k8s.
