---
"ado-preflight-ui": minor
---

AAP Hostname URL now keeps Hub Galaxy credential Server URLs (and the Hub container registry host when unset) in sync; Hub EE push gains an optional remote pull from GitHub Container Registry (`hub_ee_pull`) instead of requiring a local-only image.
