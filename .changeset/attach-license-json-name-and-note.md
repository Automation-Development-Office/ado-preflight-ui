---
"ado-preflight-ui": patch
---

License tab warns to populate General → AAP Hostname and Admin password before attach; Download JSON uses `attach-aap-license` instead of `install-aap-ocp` for license-only runs.
