---
"ado-preflight-ui": patch
---

Restore Actions → Download Vault JSON / Push Vault JSON to Git / Upload JSON → Push Vault to Git. Encrypts with ansible-vault as `ado-preflight-<env>.json.vault.yml` and pushes to the Project Git repo from Git Configuration.
