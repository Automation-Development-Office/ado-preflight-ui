---
"ado-preflight-ui": minor
---

Add AWS platform component with EC2 AMI copy bootstrap configuration (shared AWS credentials, ec2_ami_copy job options, and component_apps/component_options wiring).
