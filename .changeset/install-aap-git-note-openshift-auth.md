---
"ado-preflight-ui": patch
---

Keep OpenShift API host/token when Install AAP is on without an OpenShift component, add an AAP version selector on Install / Run, and link back to Core Environment Git Configuration.
