---
"ado-preflight-ui": patch
---

Add prod to Additional Environments (default checked), stop leftover Install AAP JT when Install AAP is off, move collection install to a script file so logs no longer dump ERROR! script text, and warn when Hub collection update is disabled.
