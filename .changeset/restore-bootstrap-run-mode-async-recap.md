---
"ado-preflight-ui": patch
---

Hub-only and standalone AAP runs ignore imported ``git.auto_push: true``; server normalizes to false, collection forces manual git mode, and vault encrypt duplicate-id fix is included.
