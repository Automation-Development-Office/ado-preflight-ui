---
"ado-preflight-ui": minor
---

Add a thin Airgap Architect adapter (`POST /api/airgap-architect/map` plus Agent Installer handoff buttons), default Hub EE image name to registry-safe `ado-ee` (Contoller EE can stay `ADO-ee`), and GitLab/Grafana standalone config panels with lab hostname/IP/password defaults.
