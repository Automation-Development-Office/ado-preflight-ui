---
"ado-preflight-ui": patch
---

Fix hub-only Galaxy credentials: reload aap_config_vars before apply (stale configs/controller was skipping create/attach). Clarify per-cred org attach checkbox. Ship infra.ado 1.1.3 + updated docker overlays.
