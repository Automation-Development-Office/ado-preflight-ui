---
"ado-preflight-ui": patch
---

Autofill Grafana OIDC client ID and issuer from RHBK when both components are selected; client secret is fetched at deploy (same as OpenShift OAuth) unless manually overridden.
