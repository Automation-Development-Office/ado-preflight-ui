---
"ado-preflight-ui": minor
---

Git TLS skip control, Bitbucket Bearer git auth, optional Hub EE push and Galaxy credential setup, Satellite manifest upload, Credentials card, Hub collection force/update-only modes, provision/OpenShift Virt fixes, and release workflow Changesets support (version tag only on pre-releases for GHCR `:latest`).
