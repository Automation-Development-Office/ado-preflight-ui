name: Release Container

on:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ado-preflight-ui

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set GHCR image name
        run: echo "GHCR_IMAGE=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Generate and update changelog
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api \
            "repos/${{ github.repository }}/releases/generate-notes" \
            -f tag_name="${{ github.event.release.tag_name }}" \
            -f target_commitish="${{ github.event.release.target_commitish }}" \
            --jq .body > changelog.md

          gh release edit "${{ github.event.release.tag_name }}" --notes-file changelog.md

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build container image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Containerfile
          tags: |
            ${{ env.GHCR_IMAGE }}:${{ github.event.release.tag_name }}
            ${{ env.GHCR_IMAGE }}:latest
          load: true

      - name: Push container image
        run: |
          docker push "${{ env.GHCR_IMAGE }}:${{ github.event.release.tag_name }}"
          docker push "${{ env.GHCR_IMAGE }}:latest"

      - name: Export container image
        run: |
          docker save "${{ env.GHCR_IMAGE }}:${{ github.event.release.tag_name }}" \
            | gzip > "${{ env.IMAGE_NAME }}-${{ github.event.release.tag_name }}.tar.gz"

      - name: Upload container artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.IMAGE_NAME }}-${{ github.event.release.tag_name }}
          path: ${{ env.IMAGE_NAME }}-${{ github.event.release.tag_name }}.tar.gz
          retention-days: 90
          if-no-files-found: error

      - name: Attach image to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            "${{ env.IMAGE_NAME }}-${{ github.event.release.tag_name }}.tar.gz" \
            --clobber
