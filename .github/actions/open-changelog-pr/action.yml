---
name: Open changelog pull request
description: Commit Changesets version output and open a PR to main

inputs:
  version:
    description: Release version (tag without v prefix)
    required: true
  tag:
    description: Git tag name (e.g. v1.2.0)
    required: true

outputs:
  pr_url:
    description: URL of the opened changelog pull request, if created
    value: ${{ steps.open_pr.outputs.pr_url }}
  pr_opened:
    description: Whether a changelog pull request was opened or already existed
    value: ${{ steps.open_pr.outputs.pr_opened }}

runs:
  using: composite
  steps:
    - name: Open changelog pull request to main
      id: open_pr
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        set -euo pipefail
        VERSION="${{ inputs.version }}"
        TAG="${{ inputs.tag }}"
        BRANCH="changelog/release-${VERSION}"
        REMOTE_REF="refs/heads/${BRANCH}"
        HEAD_REF="${GITHUB_REPOSITORY_OWNER}:${BRANCH}"

        echo "pr_opened=false" >> "$GITHUB_OUTPUT"

        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

        git fetch origin main

        if ! git diff --quiet origin/main -- CHANGELOG.md package.json package-lock.json .changeset/ \
          || [ -n "$(git ls-files --others --exclude-standard .changeset/)" ]; then
          echo "Changelog/version changes detected relative to origin/main."
        else
          echo "No changelog changes relative to origin/main; skipping PR."
          exit 0
        fi

        git add -A CHANGELOG.md package.json package-lock.json .changeset/
        if git diff --cached --quiet; then
          echo "No staged changelog changes to commit."
          exit 0
        fi

        git commit -m "Changelog for release ${VERSION}"

        if git ls-remote --exit-code --heads origin "${BRANCH}" >/dev/null 2>&1; then
          echo "Branch ${BRANCH} already exists; updating it."
          git push origin "HEAD:${REMOTE_REF}" --force
        else
          git push origin "HEAD:${REMOTE_REF}"
        fi

        for attempt in 1 2 3 4 5; do
          EXISTING_PR="$(gh pr list \
            --repo "${GITHUB_REPOSITORY}" \
            --head "${HEAD_REF}" \
            --base main \
            --state open \
            --json url \
            --jq '.[0].url' 2>/dev/null || true)"
          if [ -n "${EXISTING_PR}" ]; then
            echo "Changelog PR already exists: ${EXISTING_PR}"
            {
              echo "pr_url=${EXISTING_PR}"
              echo "pr_opened=true"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/${BRANCH}" >/dev/null 2>&1; then
            break
          fi
          echo "Waiting for branch ${BRANCH} to become visible (attempt ${attempt})..."
          sleep 2
        done

        PR_BODY="Auto-generated changelog for GitHub Release **${TAG}**.

        Merge this PR to update \`CHANGELOG.md\` / \`package.json\` on \`main\` and remove consumed Changesets."

        PR_URL="$(gh pr create \
          --repo "${GITHUB_REPOSITORY}" \
          --base main \
          --head "${BRANCH}" \
          --title "Changelog for release ${VERSION}" \
          --body "${PR_BODY}")"
        echo "Opened changelog PR: ${PR_URL}"
        {
          echo "pr_url=${PR_URL}"
          echo "pr_opened=true"
        } >> "$GITHUB_OUTPUT"
